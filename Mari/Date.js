const campo = document.querySelector('#date');

window.addEventListener(function() {
    var data = new Date(); 
    campo.min = formata(i<=16/03/1981);
    data.setFullYear(data.getFullYear());
    campo.max = formata(i<=16/03/2021);
});